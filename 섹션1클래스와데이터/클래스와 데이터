## 클래스가 필요한 이유
코드 안에는 여러가지 데이터가 있다.
이 데이터들을 나누어서 관리하는 것보다 하나의 개념으로 나눠서 관리하는 것이 편리하다
클래스는 이 개념들을 만드는 것이다.
자바에는 기본 타입이 있는데 클래스를 사용하면 int, string 같은 타입을 만들 수 있다.
사용자 정의 타입을 만드려면 설계도가 필요한데, 설계도가 바로 클래스 이다.
실제 메모리에 만들어진 실체를 객체 또는 인스턴스이다.
